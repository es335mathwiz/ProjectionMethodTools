package gov.frb.ma.msu.ProjectionTools;
//import no.uib.cipr.matrix.sparse.*;
//import no.uib.cipr.matrix.*;
//import java.io.Reader; 
//import java.io.IOException; 
 

public class useMtj {
    public void doComp()  throws Exception{
	/*
	CompRowMatrix aa = new CompRowMatrix(2,2);
	DenseVector xx = new DenseVector(2);
	DenseVector bb = new DenseVector(2);
	IterativeSolver solver = new CG(xx);
	Preconditioner mm= new ICC(aa.copy());
	StringBuffer buf = new StringBuffer(); 
	//	MatrixVectorReader mvReader = new MatrixVectorReader(buf,100);
	int ii,jj;

	mm.setMatrix(aa);
	solver.setPreconditioner(mm);
	solver.getIterationMonitor().
	    setIterationReporter(new OutputIterationReporter());
	*/
    }
}
